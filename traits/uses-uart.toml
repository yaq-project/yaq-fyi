trait = "uses-uart"
doc = """
Device which communicates over a serial bus with a given baud rate (e.g. /dev/ttyACM0 on unix and COM3 on windows at 9600 baud)
"""
requires = ["uses-serial"]

[config]

[config.serial_port]
type = "string"

[config.baud_rate]
type = "int"